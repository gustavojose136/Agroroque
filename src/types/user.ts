export type User = {
    id: number,
    nome: string,
    email?: string,
    password?: string,
    pic?: string,
    token?: string,
    phone?: string,
    role?: number
    roleName?:string,
    perfilUsuario?: number
};
  